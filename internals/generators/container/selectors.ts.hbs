import { createSelector } from '@reduxjs/toolkit'; import { RootState } from
'store/types'; import { initialState } from './slice'; 
const domains = {
  root: (state: RootState) => state.{{camelCase ComponentName}}|| initialState
} 
export const {{properCase ComponentName}}selectors = { root: createSelector( [domains.root],
(state) => state,) }